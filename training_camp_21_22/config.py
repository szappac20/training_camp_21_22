import os

base_folder = os.path.dirname(os.path.abspath(__file__))
data_path = os.path.join(base_folder, "..", "data")
plot_path = os.path.join(base_folder, "..", "plots")
submission_path = os.path.join(base_folder, "..", "submissions")
